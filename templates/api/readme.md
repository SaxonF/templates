# Data API template

PostgREST API configuration for local development.

## Configuration

This template installs `supabase/config/api.toml` — a partial config fragment. Merge its sections into `supabase/config.toml` alongside any other fragments under `supabase/config/*.toml`.

## Dependencies

Requires **database**.
