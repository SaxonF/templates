# Local development: copied to supabase/functions/.env (auto-loaded by `supabase start`).
# Hosted projects: set secrets with `supabase secrets set KEY=value`.

OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-5.4-mini
